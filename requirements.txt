# Telegram library. Exactly one MTProto library must be installed —
# pyrofork and pyrogram share the `pyrogram` namespace, and installing both
# makes which one loads at runtime nondeterministic.
pyrofork==2.3.60
# pyrofork's maintained crypto fork. Ships prebuilt wheels, unlike tgcrypto
# which needs a C compiler on Python 3.12+.
TgCrypto-pyrofork==1.2.8

# MongoDB
motor==3.5.1
pymongo==4.8.0
# Required for mongodb+srv:// connection strings
dnspython==2.6.1

# Runtime
psutil==6.0.0
aiohttp==3.9.5
